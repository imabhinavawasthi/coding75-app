import LivePage from "../../(components)/live-page";

const LiveClassroomPage = () => {
    return (
        <div>
            <LivePage topicname=""/>
        </div>
    );
}

export default LiveClassroomPage;