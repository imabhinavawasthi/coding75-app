import LivePage from "../../(components)/live-page";

const ProjectsPage = () => {
    return (
        <div>
            <LivePage topicname="project"/>
        </div>
    );
}

export default ProjectsPage;